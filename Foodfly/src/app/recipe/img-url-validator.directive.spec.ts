import { ImgUrlValidatorDirective } from './img-url-validator.directive';

describe('ImgUrlValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgUrlValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
